import React from 'react';
import { StyleSheet } from 'react-native';
// styles
export default StyleSheet.create({
    root: {
        // alignItems: 'center',
        // alignSelf: 'center',
    },
});